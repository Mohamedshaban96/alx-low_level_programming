#include "main.h"


/**
 * _islower - check for lowecase char
 *
 * @c: chracter to be checked
 *
 *
 * Return: if false 0, if true 1
 */

int _islower(int c)
{
	if (c > 96 && c < 123)
	return (1);

	else

	return (0);
}
